plugins {
    id 'java'
    id 'maven-publish'
    //id 'dev.frozenmilk.doc' version '0.0.4'
}

group = 'org.psilynx'
version = '0.0.3'

/*
library {
    targetMachines = [machines.linux.x86_64, machines.windows.x86_64, machines.macOS.x86_64] // Choose what you need

    binaries.configureEach {
        compileTask.get().includes.from(file("src/main/native/include"))
    }

    source { from("src/main/native/cpp/jni") }
    privateHeaders { from("src/main/native/include") }
}
 */

publishing {
    repositories {
        maven {
            name = "dairyPrivate"
            url = uri("https://repo.dairy.foundation/releases")
            credentials(PasswordCredentials)

            authentication {
                basic(BasicAuthentication)
            }
        }
    }
    publications {
        maven(MavenPublication) {
            groupId = "org.psilynx"
            artifactId = "psikit"
	    
	    //artifact(dairyDoc.dokkaHtmlJar)
	    //artifact(dairyDoc.dokkaJavadocJar)

            version = "0.0.3"
            from components.java
        }
    }
}
java {
    sourceCompatibility JavaVersion.VERSION_11
    targetCompatibility JavaVersion.VERSION_11
}
repositories {
    google()
    mavenCentral()
}
dependencies {
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.12.7'
    testImplementation 'junit:junit:4.12'
}
